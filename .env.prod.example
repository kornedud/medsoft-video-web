# === Обязательные ===
DOMAIN=call.example.com
SESSION_SECRET=сгенерируйте-длинную-случайную-строку
POSTGRES_PASSWORD=сильный-пароль-базы

# === Postgres (если нужно переопределить) ===
# POSTGRES_USER=teleconsult
# POSTGRES_DB=teleconsult

# === TURN (coturn) ===
TURN_SECRET=changeme-turn-secret
TURN_USER=teleconsult
TURN_PASS=turn-password
